<? if($item instanceof \FormObject\Form): ?>
<form <?= $item->attributes?>>
<? foreach($item->fields as $field): ?>
<?= $field ?>
<? endforeach ?>
</form>
<? else: ?>
<label for="<?= $item->getId() ?>"><?= $item->getTitle() ?></label><input <?= $item->attributes ?>/>
<? endif ?>
